services:
  - type: web
    name: mastercard-fraud-detection-api
    runtime: python
    rootDir: tanishq
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
